# ModularPlatform — CRM Module Test Scenarios (UC + EC)

Coverage map + test plan for the **CRM module** (contacts, interactions, meetings) on branch
`feature/crm-module`. Each scenario is **Given / When / Then**, tagged by type and status.
Types: **U** unit (no host) · **I** integration (shared `PlatformApiFactory` + Testcontainers-Postgres) ·
**C** concurrency · **F** failure/resilience · **A** architecture (ArchUnitNET) · **E** end-to-end frontend
(Playwright over the live BFF + API).
Status: **✓** implemented · **▢** gap (no test / not built) · **◐** partially covered.

> Reuse the shared harness (`tests/ModularPlatform.IntegrationTesting`). One behaviour per test. Assert through
> HTTP/queries, never private state. See the `writing-modularplatform-tests` skill.

> **2026-06-28** — authored from a line-by-line audit of branch `feature/crm-module` (Core handlers, validators,
> entity configs/migrations, GDPR port impls, and the Next.js CRM slice). Rows cite the exact `file:line` evidence
> so this catalog doubles as a gap map. Where a row is `▢`/`◐`, the "Known issues feeding these" section at the
> end pins the concrete HIGH/MEDIUM defect behind it.

---

## Coverage at a glance

| Area | ✓ | ◐ | ▢ | Headline gaps |
|---|---|---|---|---|
| 1. Contacts — CRUD, validation, IDOR, paging | 7 | 5 | 20 | PATCH is a **full-replace** (silent data loss); status silently defaults to `lead`; page shape `total` not `totalCount`; **no `(UserId,CreatedAt)` index** behind the default sort; tags + name unqueryable; non-unique email blind index |
| 2. Interactions — append-only activity log | 3 | 4 | 18 | **No correction path** (append-only, no edit/delete); **not pageable** (>500 unreachable); no `occurredAt` sanity guard; orphan-listable after parent contact soft-delete; double-submit duplicates |
| 3. Meetings — scheduling & state machine | 9 | 9 | 11 | **No state machine** (terminal transitions silently succeed); **CompleteMeeting non-idempotent** → duplicate timeline rows; `no_show` is a dead enum; `Outcome` half-wired (UI always nulls); no DeleteMeeting slice |
| 4. GDPR, encryption-at-rest, audit-PII, RLS | 5 | 3 | 13 | **Contact-as-own-`IDataSubject` blocks user-erasure DEK shred** → audit PII stays decryptable; **free-text PII (Notes/Body/Outcome) stored plaintext in audit**; export↔erasure asymmetry (Company/Position/Tags survive); no CRM export/erasure test |
| 5. Concurrency, idempotency, data-model, perf | 8 | 4 | 13 | Missing `(UserId,CreatedAt)` index; **Tags unqueryable** (no GIN, no filter); **no FK → orphan-on-delete**; no idempotency keys (double-submit dup); status/type free `varchar(32)` with no DB CHECK |
| 6. Frontend — pages, forms, states, nav, i18n | 0 | 10 | 15 | **No `crm.spec.ts` e2e at all**; backend field errors don't bind to forms; full-replace PATCH from forms; standalone `/crm/meetings` orphaned (no nav link); complete-meeting always sends `outcome:null`; delete has no confirm |
| **Total** | **32** | **35** | **90** | 157 scenarios; the green core is contact/meeting happy-path round-trips + RLS isolation + arch boundaries — the state machine, paging contract, GDPR audit-PII shred, and the whole FE e2e layer are unproven |

---

## 1. Contacts — CRUD, validation, IDOR, paging

| # | Scenario (Given / When / Then) | Type | Status |
|---|---|---|---|
| CT-1 | Authenticated owner POSTs a full contact (name/email/phone/company/position/notes/tags/status) → `201`; GET by id round-trips with name/email/phone **decrypted at rest** (read converter), tags + status intact | I | ✓ `CrmContactsTests.Create_then_get_round_trips_including_encrypted_fields` |
| CT-2 | Owner GETs `/crm/contacts/{id}` they own → `200` + full `ContactResponse` (incl. `CreatedAt`/`UpdatedAt`) | I | ✓ `Create_then_get_round_trips_including_encrypted_fields` |
| CT-3 | **IDOR**: a different user GETs another owner's contact id → `404 crm.contact_not_found` (explicit `UserId` WHERE + RLS, not 403) | I | ✓ `Foreign_contact_is_not_found` |
| CT-4 | GET a non-existent / random GUID → `404 crm.contact_not_found` | I | ◐ (only the foreign-id path asserted — `GetContactHandler.cs:24`; pure-random-id case untested) |
| CT-5 | List owner-scoped + `?status=` exact filter + `?email=` exact blind-index match; a second user sees `total=0` | I | ✓ `List_filters_by_status_and_email_and_is_owner_scoped` |
| CT-6 | List `?company=` case-insensitive **contains** (`ILike '%term%'`) | I | ▢ (filter exists `ListContactsHandler.cs:31-35`; no test) |
| CT-7 | **Duplicate email allowed**: two contacts with the same e-mail for one user both persist; `?email=` returns both (blind index is non-unique — `Contact.cs:28-29`) | I | ▢ (no test; only single-match asserted in CT-5) |
| CT-8 | Email lookup goes through the keyed HMAC blind index (`EmailHash`), never the encrypted ciphertext column; lookup normalizes `Trim().ToUpperInvariant()` | I | ◐ (round-trip match proven in CT-5; case/whitespace-normalization equivalence untested — `ListContactsHandler.cs:39`) |
| CT-9 | Update changes name/company/status → `200` + updated `ContactResponse`; xmin + `ConcurrencyRetryBehavior` serialize edits | I | ✓ `Update_changes_fields` |
| CT-10 | **PATCH is a FULL REPLACE, not a partial patch**: a PATCH that omits `email`/`phone`/`notes`/`tags` **wipes them to null/empty** — silent data loss (`UpdateContactHandler.cs:25-33` writes every column from the command; omitted JSON → request default) | I | ▢ (data-loss gap — no test; `Update_changes_fields` only sends a subset and never asserts the untouched fields survived) |
| CT-11 | **Status silently defaults to `lead`** on PATCH/POST when the body omits/blanks `status` (endpoint maps blank → `ContactStatuses.Lead` — `UpdateContactEndpoint.cs:36`, `CreateContactEndpoint.cs:33`), so a full-replace update of a `customer` with no `status` downgrades it to `lead` | I | ▢ (silent-downgrade gap — no test) |
| CT-12 | **IDOR on update**: a non-owner PATCHes a foreign contact id → `404` (owner WHERE + soft-delete filter — `UpdateContactHandler.cs:20`) | I | ▢ (untested; only GET/interaction foreign-id paths covered) |
| CT-13 | Delete soft-deletes (`DeletedAt` stamped); the contact then 404s on GET and drops out of List (global `ISoftDeletable` filter) | I | ✓ `Delete_soft_deletes_and_hides_from_get_and_list` |
| CT-14 | Deleting an already-deleted / foreign contact → `404` (the soft-delete filter hides it from the tracked load — `DeleteContactHandler.cs:18-19`) | I | ▢ (no test; double-delete idempotency unverified) |
| CT-15 | **Soft-delete leaves interactions & meetings ORPHANED**: deleting a contact does NOT cascade — its `ContactInteraction`/`Meeting` rows remain (referenced by Id, no navigation) and ListInteractions can still resolve them via the contact-owner check | I | ▢ (orphan behaviour absent/untested — soft-delete only stamps the contact row) |
| CT-16 | **Restore / hard-delete absent**: no slice un-deletes or purges a soft-deleted contact (`DeletedAt` is never cleared anywhere; GDPR erasure scrubs but there is no admin restore) | I | ▢ (absent — no slice) |
| CT-17 | Validation: empty/whitespace `FullName` → `400 crm.contact.full_name.required`; >256 chars → `crm.contact.full_name.too_long` | U | ▢ (rules at `CreateContactValidator.cs:10-12`; no test — only `Invalid_status_is_rejected` exercises the pipeline) |
| CT-18 | Validation: invalid e-mail format → `crm.contact.email.invalid`; e-mail >256 → `too_long`; phone >64 / company >256 / position >256 / notes >8192 → matching `*.too_long` codes | U | ▢ (`CreateContactValidator.cs:14-22`; untested) |
| CT-19 | Validation: `status` outside `{lead,active,customer,archived}` → `400 crm.contact.status.invalid` (membership via `ContactStatuses.IsValid`) | I | ✓ `Invalid_status_is_rejected` |
| CT-20 | Validation: >32 tags → `crm.contact.tags.too_many`; any tag >48 chars → `crm.contact.tag.too_long` | U | ▢ (`CreateContactValidator.cs:27-28`; untested) |
| CT-21 | **Empty-string tags are NOT rejected**: `tags:["",""]` passes (the per-tag rule only caps length, no `NotEmpty`) → blank tags persist into the `text[]` column | U | ▢ (gap — `CreateContactValidator.cs:28` has no emptiness guard) |
| CT-22 | Maximal valid input: 256-char name, 32 tags of 48 chars, 8192-char notes all accepted and round-trip | I | ▢ (boundary untested) |
| CT-23 | Paging: `?limit=` clamped to `1..200` (e.g. `limit=0`→1, `limit=9999`→200), `?offset<0`→0, defaults `limit=50 offset=0` | U/I | ▢ (`ListContactsHandler.cs:20-21`; no test asserts the clamp) |
| CT-24 | Paging shape: response is `{ items, total, limit, offset }` — **`total`, NOT `totalCount`** — diverging from the platform `Paged` envelope (`ContactsPageResponse` — `ContactDtos.cs:25-29`); a generic client expecting `totalCount` breaks | I | ◐ (tests read `total` directly in CT-5/CT-13; the shape divergence itself is unguarded) |
| CT-25 | Paging window correctness: with N>limit contacts, `offset`+`limit` returns the right slice and `total` stays the full count | I | ▢ (no multi-page test) |
| CT-26 | **Default sort is fixed `CreatedAt DESC` and UNINDEXED**: no `?sort=` param exists; the order is hardcoded (`ListContactsHandler.cs:46`) and the only indexes are `(UserId,Status)` + `(EmailHash)` — there is **no `(UserId,CreatedAt)` index** (`Contact.cs:77-78`), so the default list does a sort over a UserId scan | I | ▢ (sort not configurable; index gap — no perf/sort test) |
| CT-27 | **Name search NOT supported**: name is `[Encrypted]` at rest with no blind index, so List has no name/free-text filter (only status/company/email) — searching by name is impossible | I | ▢ (absent by design — `ListContactsQuery.cs:5-10` notes a name blind index is a "later enhancement") |
| CT-28 | **Tag filter NOT supported**: tags persist but List exposes no `?tag=` filter — cannot list contacts by tag | I | ▢ (absent — no slice/param) |
| CT-29 | Identity from the token only: `UserId` is taken from `ITenantContext.UserId` in every endpoint, never the request body; a body-supplied owner id is ignored (Law 10) | I | ◐ (commands carry no body owner field — `CreateContactCommand.cs`/`CreateContactEndpoint.cs:21-22`; enforced structurally, not asserted by a dedicated test) |
| CT-30 | Audit-PII: the `[PersonalData]` name/email/phone are written to `crm_audit_entries` only as a `penc:` envelope (crypto-shredded under the contact's own DEK, `IDataSubject.SubjectId => Id`), never plaintext; after GDPR erasure they surface as `[erased]` | I | ▢ (Contact pairs `[PersonalData]`+`IDataSubject` `Contact.cs:16-43`; no Contacts-specific audit-PII test — covered generically by platform `PersonalDataConventionTests`) |
| CT-31 | Concurrency: two parallel PATCHes to one contact → one wins, the other retries via xmin/`ConcurrencyRetryBehavior` (tracked entity), no `500`, no lost-update beyond the documented full-replace overwrite | C | ▢ (untested) |
| CT-32 | Module gate: a tenant NOT entitled to `crm` calling any contacts route → `404` (`RequireModule("crm")` — `CreateContactEndpoint.cs:40`); anonymous → `401 auth.required` | I | ◐ (entitlement assumed via `ProductModuleKeys.DefaultEntitled` in the test tenant; the denied-entitlement + anonymous cases are not asserted here) |

---

## 2. Interactions — activity log (append-only)

| # | Scenario (Given / When / Then) | Type | Status |
|---|---|---|---|
| IN-1 | Own contact / `POST /crm/contacts/{id}/interactions` `{type:"call",body,occurredAt}` → `201` + `{id}`; row persisted with `UserId`/`ContactId` stamped from token + route | I | ✓ `Interactions_add_and_list_newest_first` |
| IN-2 | `GET /crm/contacts/{id}/interactions` → `200` flat array, **newest-first** (`OrderByDescending(OccurredAt)`) | I | ✓ `Interactions_add_and_list_newest_first` (asserts `items[0].type=="note"`) |
| IN-3 | Add with `occurredAt` omitted → `OccurredAt` defaults to `clock.UtcNow` (UTC) | I | ◐ (exercised in IN-1's 2nd add but never asserted — `AddInteractionHandler.cs:30`) |
| IN-4 | **IDOR/foreign contact id on add** → `404 crm.contact_not_found`; no row created | I | ✓ `Adding_interaction_to_foreign_contact_is_not_found` (owner-check `AddInteractionHandler.cs:18`) |
| IN-5 | Add to a non-existent contact id → `404 crm.contact_not_found` | I | ◐ (same `AnyAsync` guard as IN-4 covers it; no dedicated missing-id test) |
| IN-6 | Add to **own soft-deleted** contact → 404 (the `Contacts` global soft-delete filter makes `AnyAsync` false) → no orphan interaction can be created post-delete | I | ▢ (untested — `AddInteractionHandler.cs:18` relies on the soft-delete query filter) |
| IN-7 | `Type` enum membership: each of `call`/`email`/`note`/`meeting` accepted | U/I | ◐ (`call`+`note` exercised in IN-1; `email`/`meeting` never tested — `InteractionTypes.All` `ContactInteraction.cs:33`) |
| IN-8 | Invalid `Type` (e.g. `"sms"`) → `400 crm.interaction.type.invalid`; nothing persisted | U | ▢ (validator `AddInteractionValidator.cs:11`; no test) |
| IN-9 | Endpoint lower-cases + trims `Type` before validating (`"CALL "` → `"call"`) | U | ▢ (`AddInteractionEndpoint.cs:27`; untested — casing normalization could mask client bugs) |
| IN-10 | Empty/whitespace `Type` → `400 crm.interaction.type.invalid` (default `note` is NOT applied to a supplied-but-blank type; entity default only applies when the property is never set) | U | ▢ (edge: endpoint sends `""`→fails `IsValid`; "default note" only via DB default `ContactInteraction.cs:19`, not the wire path) |
| IN-11 | `Body` over 8192 chars → `400 crm.interaction.body.too_long`; max-length (=8192) accepted | U | ▢ (`AddInteractionValidator.cs:13`; no boundary test) |
| IN-12 | Whitespace-only `Body` is coalesced to `null` (not stored as `" "`) | U | ▢ (`AddInteractionHandler.cs:31`; untested) |
| IN-13 | **OccurredAt has NO date-sanity guard** — far-future (year 3000) or far-past `occurredAt` accepted verbatim → can corrupt newest-first ordering of the timeline | I | ▢ (gap — no validation rule on `OccurredAt`; `AddInteractionValidator.cs` only checks `Type`+`Body`) |
| IN-14 | List `limit` clamp: `?limit=0`/negative → clamped to **1**; `?limit=99999` → clamped to **500** | I | ▢ (`Math.Clamp(query.Limit,1,500)` `ListInteractionsHandler.cs:17`; untested) |
| IN-15 | List default limit when `?limit` omitted → **100** | I | ▢ (`limit ?? 100` `ListInteractionsEndpoint.cs:24`; untested) |
| IN-16 | **List is NOT pageable** — flat array, hard cap 500, no `offset`/`total`/`Paged<T>` envelope → a history of >500 interactions is **unreachable** (oldest silently truncated by newest-first `Take(500)`) | I | ▢ (design gap — `ListInteractionsHandler.cs:19-24` returns `IReadOnlyList`, not `Paged`; unlike `ListContacts`) |
| IN-17 | **IDOR on list / foreign contact id** → empty array (RLS + `WHERE UserId==caller`), leaks nothing (not a 404) | I | ▢ (`ListInteractionsHandler.cs:9,20`; owner-scope untested for interactions specifically) |
| IN-18 | **Append-only**: no get-single, no update, no delete slice exists → **no correction path** for a mistyped interaction (wrong contact/type/body is permanent) | I | ▢ (absent — no slice; only `AddInteraction`+`ListInteractions` ship) |
| IN-19 | **Orphan after contact soft-delete**: existing interactions remain physically present and **still listable** via the interactions endpoint after the parent contact is soft-deleted (list slice has no contact-existence/soft-delete check) | I | ▢ (gap — `ListInteractionsHandler` filters only `ContactId`+`UserId`, never joins/checks the contact's `DeletedAt`) |
| IN-20 | **GDPR erasure** scrubs interaction `Body`→`null` for all of the user's interactions (rows retained, free text gone); contacts tombstoned, never the interaction rows physically deleted | I | ▢ (`CrmPersonalDataEraser.cs:36-39`; no CRM erasure test asserts interaction-body blanking) |
| IN-21 | **GDPR export** includes the user's interactions (type/occurredAt/body) under the `Crm` key | I | ▢ (`CrmPersonalDataExporter.cs:39`; untested) |
| IN-22 | **Add-on-retry idempotency**: `AddInteraction` has NO idempotency key + NO UNIQUE constraint; under `ConcurrencyRetryBehavior` a transient retry of a committed insert would create a **duplicate** interaction — but `SaveChangesAsync` on a fresh insert raises no xmin conflict, so retries are not normally triggered; double client-submit ⇒ two rows | C | ▢ (gap — append-only insert is not dedup-guarded; `AddInteractionHandler.cs:34`. Inserts don't trigger xmin retry, so the practical risk is double-submit, not pipeline retry) |
| IN-23 | Add publishes **no integration event** (purely local activity log; no outbox, no cross-module fan-out) | I | ◐ (by-design `AddInteractionHandler` comment line 11; no negative test) |
| IN-24 | Unauthenticated add/list → `401 auth.required`; caller lacking the `crm` module entitlement → `404`/blocked (`.RequireModule("crm")`) | I | ▢ (`AddInteractionEndpoint.cs:33-34`, `ListInteractionsEndpoint.cs:27-28`; untested for interactions) |
| IN-25 | **FE**: add-interaction form (`type` select + `body`) → on success invalidates the CRM query cache (timeline refetch) + success toast; list renders newest-first with type labels | E | ▢ (no CRM Playwright spec — `frontend/e2e/` has no `crm.spec.ts`; component+invalidation exist at `contact-detail.tsx:40`, `hooks.ts:63-72`) |

---

## 3. Meetings — scheduling & state machine

| # | Scenario (Given / When / Then) | Type | Status |
|---|---|---|---|
| MT-1 | Authed user / `POST /v1/crm/meetings` with title+scheduledAt+duration (no contact) → `201` + `{id}`; `GET` shows `status="planned"`, `outcome=null` | I | ✓ `CrmMeetingsTests.Create_then_get_round_trips` (covers the contact-linked variant) |
| MT-2 | Create with an **owned** `contactId` → `201`; meeting `contactId` echoes back on read | I | ✓ `Create_then_get_round_trips` |
| MT-3 | Create with a **foreign / non-existent** `contactId` → `404 crm.contact_not_found`; no meeting row (owned-check `CreateMeetingHandler.cs:19`) | I | ✓ `Create_with_foreign_contact_is_not_found` |
| MT-4 | `scheduledAt` is normalized to UTC on write (`ToUniversalTime()`), all times UTC (`CreateMeetingHandler.cs:31`) | U/I | ◐ (behaviour shipped; no test asserts the tz-offset is rebased) |
| MT-5 | Create validator: empty/256+ title → `crm.meeting.title.{required,too_long}`; `scheduledAt==default` → `crm.meeting.scheduled_at.required`; duration ∉ 1..1440 → `crm.meeting.duration.invalid`; location>512 / notes>8192 → `…too_long` | U | ◐ only `durationMinutes=0`→`400` asserted (`Invalid_duration_is_rejected`); title/scheduledAt/length codes untested |
| MT-6 | **No date-sanity guard**: a meeting `scheduledAt` in the past (or years ahead) is accepted — only `!=default` + UTC normalize, no "future" rule (`CreateMeetingValidator.cs:13`) | U | ▢ (gap — no rule; intentional but untested) |
| MT-7 | `GET /v1/crm/meetings/{id}` for the owner → `200` full `MeetingResponse` (incl. CreatedAt/UpdatedAt) | I | ✓ `Create_then_get_round_trips` |
| MT-8 | **IDOR**: another user `GET`s the owner's meeting id → `404 crm.meeting_not_found` (owner predicate + RLS, `GetMeetingHandler.cs:17`) | I | ✓ `Foreign_meeting_is_not_found` |
| MT-9 | `GET` a random/non-existent meeting id → `404 crm.meeting_not_found` | I | ◐ (same 404 path as MT-8; not separately asserted) |
| MT-10 | `ListMeetings` default → owner's meetings ordered **soonest-first** (`OrderBy ScheduledAt`, `ListMeetingsHandler.cs:45`) | I | ✓ `List_is_ordered_soonest_first_and_filtered_by_status` |
| MT-11 | `ListMeetings?status=planned` filters by status (trimmed+lowercased); `?from`/`?to` window + `?contactId` filter narrow the set | I | ◐ `status` asserted; `from`/`to`/`contactId` filters untested (`ListMeetingsHandler.cs:21-34`) |
| MT-12 | Paging bounds: `limit` clamped to 1..200, `offset` floored at 0; out-of-range page → empty `items`, `total` unchanged | I | ▢ (clamp at `ListMeetingsHandler.cs:16`; untested) |
| MT-13 | **Paging shape divergence**: list returns `{items,total,limit,offset}` (`MeetingsPageResponse`) — NOT the platform-canonical `PagedResponse{Items,Page,PageSize,TotalCount}` (offset/limit vs 1-based page; cap 200 vs 100) `MeetingDtos.cs:17` vs `Cqrs/Paging.cs:7` | A | ▢ (intentional bespoke DTO; FE reads `data.total`/`items`) |
| MT-14 | `PATCH /v1/crm/meetings/{id}` reschedules/edits title/time/duration/location/notes → `200` updated meeting | I | ✓ `Update_reschedules_the_meeting` |
| MT-15 | Update does **NOT** touch `Status` or `Outcome` (a "done" meeting stays done after an edit) (`UpdateMeetingHandler.cs:18-22`) | I | ◐ (source-evident — no field assigned; no test edits a terminal meeting to prove it) |
| MT-16 | Update **cannot change `ContactId`**: the command/request carries no contactId, so a meeting's contact link is immutable after create (`UpdateMeetingCommand.cs`) | I | ▢ (by omission; untested) |
| MT-17 | Foreign user `PATCH`es the owner's meeting → `404 crm.meeting_not_found` (`UpdateMeetingHandler.cs:15`) | I | ▢ (gap — no cross-user update test) |
| MT-18 | `POST /{id}/complete` with outcome → `204`; meeting `status="done"`, `outcome` stored; linked contact gets a `type="meeting"` interaction on its timeline (`CompleteMeetingHandler.cs:22-34`) | I | ✓ `Complete_marks_done_and_logs_interaction_on_contact` |
| MT-19 | Complete an **unlinked** meeting (no contactId) → `204`, status done, **no** interaction inserted | I | ▢ (gap — only the linked path is tested) |
| MT-20 | **Complete is NOT idempotent**: completing twice flips done→done but **inserts a second `meeting` interaction** each call → duplicated timeline rows (no dedup key, `CompleteMeetingHandler.cs:27`) | I | ◐ known bug (no test; double-submit duplicates) |
| MT-21 | **ConcurrencyRetryBehavior re-run also duplicates**: a transient xmin/save retry re-executes `Handle`, re-adding the `ContactInteraction` (tracker cleared between retries) → phantom duplicate interaction on a single complete | C | ◐ known bug (interaction add is not guarded by a UNIQUE/idempotency key) |
| MT-22 | **No state machine — terminal transitions silently succeed**: complete an already-`canceled` meeting, cancel a `done` meeting, or `PATCH`-reschedule a terminal meeting all return success with no guard (handlers blindly set `Status`) | I | ◐ known design gap (`Cancel/Complete/UpdateMeetingHandler` have no status precondition) |
| MT-23 | `POST /{id}/cancel` → `204`, status `canceled`; re-cancel is a no-op (doc-claimed "idempotent" — really just re-sets `canceled`, `CancelMeetingHandler.cs:18`) | I | ✓ cancel exercised by `List_is_ordered_soonest_first_and_filtered_by_status`; re-cancel path untested |
| MT-24 | Foreign user `POST`s `/cancel` or `/complete` on the owner's meeting → `404 crm.meeting_not_found` | I | ▢ (gap — no cross-user mutation test) |
| MT-25 | **`no_show` status is UNREACHABLE (dead)**: it's a valid `MeetingStatuses` value + an FE filter/badge, but no slice ever assigns it (`Meeting.cs:36`, `meetings-table.tsx:23`) | A | ▢ (dead enum member — present, never set) |
| MT-26 | **`Outcome` is half-wired**: the FE never collects it — the complete button sends `outcome:null` (`meetings-table.tsx:89`) and the form dialog has no outcome field; only an API caller can persist an outcome | E | ◐ (back-end stores it — MT-18; UI always nulls it) |
| MT-27 | **No `DeleteMeeting` slice**: `Meeting` is `ISoftDeletable` but no endpoint/handler soft-deletes a meeting (only GDPR erasure scrubs it) | I | ▢ (absent — no slice) |
| MT-28 | GDPR erasure: a user's meeting metadata (title/location/notes/outcome — plain columns, not `[Encrypted]`) is scrubbed by the CRM eraser; rows survive as audit-safe records (`Meeting.cs:7-11`) | I | ▢ (eraser behaviour for meetings untested in this area — see GDPR section) |
| MT-29 | Audit: a meeting mutation (reschedule/complete/cancel) writes only changed columns to `crm_audit_entries` via the interceptor (`AuditableEntity` + xmin) | I | ▢ (gap — no meeting-specific audit assertion) |

---

## 4. GDPR, encryption-at-rest, audit-PII, RLS isolation

| # | Scenario (Given / When / Then) | Type | Status |
|---|---|---|---|
| GD-1 | **PII encrypted at rest**: create a contact → `crm_contacts.full_name/email/phone` hold a `penc:v2` envelope (never plaintext); GET decrypts via the read-factory model converter → original values returned | I | ✓ `CrmContactsTests.Create_then_get_round_trips_including_encrypted_fields` (round-trip proven; raw-column-is-ciphertext not separately asserted — `Contact.cs:20-33`) |
| GD-2 | **Blind-index e-mail lookup**: `EmailHash = HMAC(email.Trim().ToUpperInvariant())` (`CreateContactHandler.cs:26`); list `?email=` filters on the hash, never the ciphertext column | I | ✓ `CrmContactsTests.List_filters_by_status_and_email_and_is_owner_scoped` (exact-match hit; case/whitespace-normalization edge not separately asserted) |
| GD-3 | Blind index is keyed platform-wide (login-style pre-auth key), non-UNIQUE — one user may hold duplicate same-email contacts, both retrievable by hash | I | ▢ (no duplicate-email test; `EmailHash` index non-unique — `Contact.cs:78`) |
| GD-4 | **RLS read isolation (contact)**: user B GETs user A's contact id → `404` (foreign id is invisible, not `403`) | I | ✓ `CrmContactsTests.Foreign_contact_is_not_found` |
| GD-5 | **RLS list isolation**: a second user lists contacts → A's rows excluded (`total=0`), no cross-user leak | I | ✓ `CrmContactsTests.List_filters_by_status_and_email_and_is_owner_scoped` (other user sees `total=0`) |
| GD-6 | **RLS isolation (meeting)**: user B GETs A's meeting id → `404`; creating a meeting against A's contact id → `404` | I | ✓ `CrmMeetingsTests.Foreign_meeting_is_not_found` + `Create_with_foreign_contact_is_not_found` |
| GD-7 | **RLS isolation (interaction)**: user B adds/lists interactions on A's contact → `404` (foreign contact invisible) | I | ◐ add-path covered (`CrmContactsTests.Adding_interaction_to_foreign_contact_is_not_found`); foreign **list** interactions path not separately tested |
| GD-8 | Tenant scoping: all three entities are `ITenantScoped` → cross-tenant rows excluded by the global filter even before per-user RLS | I | ▢ (relies on platform `TenantIsolationTests`; no CRM-specific cross-tenant test) |
| GD-9 | **GDPR export**: `CrmPersonalDataExporter` returns `contacts`+`interactions`+`meetings` for the userId, PII decrypted via the read factory, `IgnoreQueryFilters` so soft-deleted rows still export | I/F | ▢ (absent — no CRM GDPR/export test exists; `CrmPersonalDataExporter.cs`) |
| GD-10 | Export field-completeness: contacts include Company/Position/Notes/Tags/Status; interactions include Body; meetings include Notes/Outcome/Location | I | ▢ (untested — `CrmPersonalDataExporter.cs:24-61`) |
| GD-11 | **GDPR erasure**: publish `UserErasureRequested` → Worker fans out `CrmPersonalDataEraser`; contacts tombstoned (`FullName="[erased]"`, `Email/EmailHash/Phone/Notes` nulled) + soft-deleted; interaction `Body` nulled; meetings tombstoned + soft-deleted | I | ▢ (absent — no CRM erasure test; `CrmPersonalDataEraser.cs:19-53`) |
| GD-12 | Erasure idempotency: a second `EraseAsync` for the same userId is a no-op-equivalent (`DeletedAt ?? now` preserves the first timestamp; values already neutral) | I | ▢ (untested — `CrmPersonalDataEraser.cs:33,51`) |
| GD-13 | Erasure uses `ExecuteUpdate` + `IgnoreQueryFilters` → already-soft-deleted contacts still holding ciphertext are caught and tombstoned | I | ▢ (untested — `CrmPersonalDataEraser.cs:24`) |
| GD-14 | **BLOCKER — Contact is its own `IDataSubject` (`SubjectId => Id`, `Contact.cs:43`)**: user-erasure shreds the USER's DEK, never each contact's per-row DEK → the contact PII ciphertext in `crm_audit_entries` stays **permanently decryptable** after erasure (audit-PII crypto-shred guarantee violated for CRM) | I | ▢ (gap — no eraser path shreds contact subject keys; cross-review blocker) |
| GD-15 | **Free-text PII survives audit after erasure**: `Contact.Notes`, `ContactInteraction.Body`, `Meeting.Notes`/`Outcome` are NOT `[PersonalData]` → `AuditInterceptor` writes them as **plaintext** into `crm_audit_entries`; the eraser's `ExecuteUpdate` bypasses the interceptor and never touches audit rows → original free-text PII remains readable post-erasure | I | ▢ (gap — `ContactInteraction.cs:22`, `Meeting.cs:22-25`, plaintext columns) |
| GD-16 | **Eraser does NOT null Company/Position**: erasure leaves `crm_contacts.company`/`position` intact (lower-sensitivity business attributes by design — `Contact.cs:35-36`); confirm this is the intended residual | I | ▢ (untested; design decision — eraser omits both — `CrmPersonalDataEraser.cs:27-33`) |
| GD-17 | **Export↔erasure asymmetry**: the exporter emits Company/Position/Notes/Tags but the eraser nulls only Notes — Company/Position/Tags are exported PII that survive erasure | I | ▢ (gap — exporter `:30-33` vs eraser scope) |
| GD-18 | Erasure leaves `crm` ledger-equivalent/append-only data intact: no physical row deletes (soft-delete only); meeting/interaction Ids preserved for referential history | I | ▢ (untested — soft-delete via `DeletedAt`) |
| GD-19 | Update re-seals + re-indexes PII: PATCH changing FullName/Email writes a new `penc:v2` envelope and recomputes `EmailHash`; old blind-index value no longer matches | I | ◐ update mutates fields (`CrmContactsTests.Update_changes_fields`) but EmailHash recompute on update not asserted (UpdateContactHandler not shown to rehash) |
| GD-20 | `[PersonalData]`⇔`IDataSubject` pairing (ArchUnitNET): `Contact` carries both; `ContactInteraction`/`Meeting` carry neither `[PersonalData]` nor `IDataSubject` (so their free-text PII is deliberately unencrypted/un-shreddable) | A | ◐ platform `PersonalDataConventionTests` enforces the pairing globally; no CRM-specific assertion that interaction/meeting free-text is intentionally excluded |
| GD-21 | Erased contact still readable as `[erased]` (not a crash): post-erasure GET/list of a tombstoned-but-soft-deleted contact is hidden by the soft-delete filter (404), export still surfaces `FullName="[erased]"` | I | ▢ (untested interaction of soft-delete filter + erasure tombstone) |

---

## 5. Concurrency, idempotency, data-model & performance

| # | Scenario (Given / When / Then) | Type | Status |
|---|---|---|---|
| DM-1 | **xmin optimistic concurrency on contact edit**: two parallel `PATCH /v1/crm/contacts/{id}` on one tracked contact → both resolve, no lost update / no unhandled 500 (xmin token + `ConcurrencyRetryBehavior` 5× backoff serialize them) | C | ▢ (mechanism inherited — `xmin` rowVersion in `CrmContacts.cs:53`, retry behaviour is platform; no CRM concurrency test) |
| DM-2 | **UpdateContact retry-safe (idempotent field-set)**: `UpdateContactHandler` assigns the WHOLE field set (`UpdateContactHandler.cs:25-33`), so a `ConcurrencyRetryBehavior` re-run after a tracker clear converges to the same row — replaying the command yields one final state | C | ▢ (no test; full-replace makes it commutative by construction) |
| DM-3 | **UpdateMeeting retry-safe**: `UpdateMeetingHandler` likewise sets title/time/duration/location/notes wholesale (`UpdateMeetingHandler.cs:18-22`); a retry re-applies the same values → no drift. (Status is deliberately untouched here) | C | ▢ (no test) |
| DM-4 | **CompleteMeeting NOT idempotent under retry**: `CompleteMeetingHandler` `db.ContactInteractions.Add(...)` on every run (`CompleteMeetingHandler.cs:27`); a concurrency retry (tracker cleared, handler re-executed) re-inserts the "meeting" interaction → **duplicate timeline rows**. No UNIQUE/dedup key guards it | C | ◐ (happy path only — `CrmMeetingsTests.Complete_marks_done_and_logs_interaction_on_contact`; duplicate-on-retry untested & undefended) |
| DM-5 | **AddInteraction double-submit duplicates**: a client retrying `POST /contacts/{id}/interactions` (or a network re-send) inserts a second row — `AddInteractionHandler` has no idempotency key, just `Add` + `SaveChanges` (`AddInteractionHandler.cs:25-35`) | C | ▢ (absent — no idempotency UNIQUE key on `crm_contact_interactions`) |
| DM-6 | **CreateContact double-submit duplicates**: re-posting the same body creates two contacts (non-unique `EmailHash` is by design — `Contact.cs:28`); there is no per-request idempotency key | I/C | ▢ (absent — no idempotency key; duplicate creation is accepted) |
| DM-7 | **Audit records ONLY changed fields**: edit one contact column → the per-module `crm_audit_entries` JSONB delta contains just that column (platform `AuditInterceptor` on `SaveChanges`) | I | ▢ (inherited interceptor; no CRM test asserts the delta shape) |
| DM-8 | **Encrypted PII never lands in audit plaintext**: `[PersonalData]+[Encrypted]` name/e-mail/phone are crypto-shredded under the contact's own DEK in the audit trail; on GDPR erasure the audit values read `[erased]` | I | ▢ (inherited; round-trip decrypt is covered by `CrmContactsTests.Create_then_get_round_trips_including_encrypted_fields`, but the audit-PII path is untested in CRM) |
| DM-9 | **Bulk `ExecuteUpdate`/`ExecuteDelete` bypass audit + xmin**: any future CRM slice using them skips the interceptor and the concurrency token. CRM today uses tracked saves only (no `ExecuteUpdate` in any handler) → currently safe | A/I | ▢ (no test asserting the absence; verified by reading — handlers all use `SaveChangesAsync`) |
| DM-10 | **IDs are Guid v7 (time-ordered)**: every CRM entity Id is `Guid.CreateVersion7()` via the `Entity` base, so default newest-first ordering is monotonic with creation | U | ▢ (inherited base; no CRM-specific assertion) |
| DM-11 | **snake_case table names**: `crm_contacts`, `crm_contact_interactions`, `crm_meetings` | A/I | ✓ (verified in `CrmContacts.cs` / `CrmMeetings.cs` migrations; convention enforced by `PlatformDbContext`) |
| DM-12 | **Meeting list index coverage**: `GET /meetings` orders by `ScheduledAt` for one user → served by `IX_crm_meetings_UserId_ScheduledAt` (no sort step) | I | ✓ index present (`CrmMeetings.cs:52`); ordering proven by `CrmMeetingsTests.List_is_ordered_soonest_first_and_filtered_by_status` |
| DM-13 | **Interaction timeline index coverage**: `GET /contacts/{id}/interactions` newest-first → served by `IX_crm_contact_interactions_ContactId_OccurredAt` | I | ✓ index present (`CrmContacts.cs:65`); order proven by `CrmContactsTests.Interactions_add_and_list_newest_first` |
| DM-14 | **Contact default-sort index MISSING → sequential sort**: `ListContactsHandler` orders by `CreatedAt` (`ListContactsHandler.cs:46`) but the only indexes are `(UserId,Status)` and `(EmailHash)` — no `(UserId,CreatedAt)`, so the default unfiltered list does a filter-then-sort (in-memory sort node), degrading as a user's contacts grow | I | ▢ (known gap — no `(UserId, CreatedAt)` index in `ContactConfiguration`/migration) |
| DM-15 | **Contact status-filter index coverage**: `GET /contacts?status=customer` → served by `IX_crm_contacts_UserId_Status` | I | ✓ index present (`CrmContacts.cs:89`); filter proven by `CrmContactsTests.List_filters_by_status_and_email_and_is_owner_scoped` |
| DM-16 | **Contact e-mail blind-index lookup**: `GET /contacts?email=…` HMACs the address and matches `EmailHash` → served by `IX_crm_contacts_EmailHash`; the ciphertext column is never queried by value | I | ✓ index present (`CrmContacts.cs:79`); proven by `CrmContactsTests.List_filters_by_status_and_email_and_is_owner_scoped` |
| DM-17 | **Tags are write-only / unqueryable**: `Tags text[]` round-trips on create+get but has **no GIN index and no filter slice** — there is no `?tag=` query path, so tags cannot drive list filtering | I | ▢ (absent — `Tags` stored `text[]` with no index, `Contact.cs:74`; round-trip only in `CrmContactsTests.Create_then_get_round_trips…`) |
| DM-18 | **status/type free `varchar(32)`, NO DB CHECK**: membership is enforced only at the edge (`ContactStatuses.IsValid` / `MeetingStatuses.IsValid` / `InteractionTypes.IsValid`). A write that bypasses the validator (e.g. a future bulk path) could persist an out-of-set status — the DB would accept it | U/I | ◐ (edge validation proven: `CrmContactsTests.Invalid_status_is_rejected`; DB-level backstop absent — no CHECK constraint in any migration) |
| DM-19 | **Meeting duration / required-field bounds**: `durationMinutes <= 0` → `400`; title required; (no upper duration cap asserted) | U/I | ◐ (`CrmMeetingsTests.Invalid_duration_is_rejected` covers `0`; max-bound/negative-elsewhere untested) |
| DM-20 | **Pagination bounds**: `ListContactsHandler` clamps `limit` to 1..200 and `offset ≥ 0` (`ListContactsHandler.cs:20-21`) → a caller can never request an unbounded/negative page; `total` reflects the full filtered count | U/I | ◐ (clamp logic present; happy-path `total` asserted in list tests, but the clamp boundaries themselves are untested) |
| DM-21 | **No FK → orphan risk on delete**: there are NO foreign keys between `crm_meetings`/`crm_contact_interactions` and `crm_contacts`. Soft-deleting a contact leaves its meetings + interactions pointing at a hidden `ContactId`; a hard contact removal would dangle them. `Meeting.ContactId` is also nullable/unvalidated against existence after creation | I | ▢ (orphan behaviour undefined/untested — soft-delete hiding proven by `CrmContactsTests.Delete_soft_deletes_and_hides…`, but linked rows are not reconciled) |
| DM-22 | **No partial index for `DeletedAt IS NULL`**: the soft-delete global filter adds `DeletedAt IS NULL` to every contact/meeting query, but no filtered/partial index supports it → tombstoned rows are still scanned | I | ▢ (absent — plain indexes only; no `WHERE DeletedAt IS NULL` partial index) |
| DM-23 | **RLS owner-column convention**: every CRM `IUserOwned` entity (`Contact`, `ContactInteraction`, `Meeting`) exposes a public `Guid UserId` so `RlsBootstrapper`'s hardcoded policy column resolves → no startup DDL crash | A | ✓ `RlsConventionTests.Every_IUserOwned_entity_exposes_a_Guid_UserId` (CRM assembly included) |
| DM-24 | **`[PersonalData]` ↔ `IDataSubject` pairing**: `Contact`'s encrypted name/e-mail/phone are `[PersonalData]+[Encrypted]` string properties on an `IDataSubject` entity (subject = its own Id, `Contact.cs:43`) | A | ✓ `PersonalDataConventionTests.Every_type_with_a_PersonalData_property_implements_IDataSubject` + `Every_Encrypted_property_is_PersonalData_on_an_IDataSubject_string` |
| DM-25 | **No cross-module Core reference / no cross-table JOIN**: CRM Core is `internal`, references other modules only via `*.Contracts`, and references entities by `Guid` Id (no navigation properties) | A | ✓ `ModuleBoundaryTests.No_module_core_depends_on_another_modules_core` + `Contracts_assemblies_must_not_depend_on_infrastructure` (Crm + Crm.Contracts loaded) |

---

## 6. Frontend — pages, forms, states, nav, i18n

| # | Scenario (Given / When / Then) | Type | Status |
|---|---|---|---|
| UI-1 | Contacts list: tenant has `crm` entitlement → `/crm` renders the contacts `DataTable` (name link, email, company, status badge) prefetched server-side | E | ▢ (untested — no `e2e/crm.spec.ts`; page `app/(tenant)/crm/page.tsx` + `ContactsTable` ship) |
| UI-2 | Status filter: pick "lead" in the `Select` → query refetches with `?status=lead`, page resets to 1 | E | ▢ (untested; `contacts-table.tsx:103-106` resets page on change) |
| UI-3 | Pagination: `total` > pageSize(20) → next/prev page buttons drive `setPage`; `offset = (page-1)*20` sent to API | E | ▢ (untested; `api.ts:92-94` `pageToOffset`) |
| UI-4 | Pagination bound: API returns `total` but `DataTable` is driven by `data.total` from `ContactsPage` — server field is `total` (not `totalCount`) so paging works only if BFF maps it | E | ◐ (depends on `ContactsPage.total` mapping `api.ts:35`; platform-wide `total`→`totalCount` mismatch is the known paging trap) |
| UI-5 | Empty state: no contacts → `emptyTitle`/`emptyDescription` shown instead of rows | E | ▢ (untested; `contacts-table.tsx:140-141`) |
| UI-6 | Loading state: initial fetch in flight → `DataTable isLoading` skeleton; contact detail shows `<Skeleton>` (`contact-detail.tsx:137`) | E | ▢ (untested) |
| UI-7 | Create contact: open `ContactFormDialog`, fill RHF+zod form, submit → POST `/crm/contacts`, dialog closes, CRM root invalidated, success toast | E | ▢ (untested; `hooks.ts:27-37`, `contact-form-dialog.tsx`) |
| UI-8 | Edit contact: dialog seeded from `contact` via `toFormValues`, PATCH `/crm/contacts/{id}`, list+detail refetch | E | ◐ (untested; PATCH is full-replace — any field the form omits is wiped server-side; FE always sends the whole `ContactInput`) |
| UI-9 | Zod client validation: empty `fullName` / invalid email / >256 char name → field error text, no network call (`noValidate` form) | E | ▢ (untested; `schema.ts:8-27`) |
| UI-10 | **Backend field-validation errors do NOT bind to the form**: API 422 `errors:[{field:"FullName",…}]` → catch treats `fieldErrors` as `Record<string,string[]>` + uses PascalCase keys → `setError` never matches camelCase RHF fields → no per-field message | E | ◐ (bug — `contact-form-dialog.tsx:96-101` + `meeting-form-dialog.tsx:85-90`; client maps `fieldErrors = problem.errors` which is an **array** `client.ts:95`, so `Object.keys` yields indices, and `field` is PascalCase) |
| UI-11 | **Zod tag validator does not mirror backend**: client only caps the raw tags string at 512 chars (`schema.ts:25`); backend per-tag rules (max 32 chars / ≤48 tags) are unmirrored → over-long/too-many tags pass client, fail server, and per UI-10 the error is invisible | E | ◐ (gap — `schema.ts:25` vs backend tag validator) |
| UI-12 | Tags round-trip: comma string split/trim/dedupe-empty on submit (`toInput`), joined back with `, ` on edit (`toFormValues`) | E | ▢ (untested; `contact-form-dialog.tsx:59-64,46`) |
| UI-13 | Contact detail: info card + interactions timeline (icon per type, localized type badge, `toLocaleString` time) + meetings section all from one `[id]` page | E | ▢ (untested; `contact-detail.tsx`) |
| UI-14 | Add interaction: inline form (type `Select` + body) → POST `/crm/contacts/{id}/interactions`, timeline refetches, form resets to `{type:"note"}` | E | ▢ (untested; `contact-detail.tsx:40-91`, `hooks.ts:63-73`) |
| UI-15 | Timeline empty/loading: no interactions → `timeline.empty`; in flight → `<Skeleton>` | E | ▢ (untested; `contact-detail.tsx:97-103`) |
| UI-16 | Foreign / non-existent contact id: API 404 (RLS-scoped) → `contact` query returns null → `contacts.notFound` copy (no crash); the route itself does not call `notFound()` on a missing contact | E | ◐ (untested; only entitlement gates `notFound()` `[id]/page.tsx:27`; missing-contact handled as a null render `contact-detail.tsx:140-141`) |
| UI-17 | Meetings on contact detail: `MeetingsTable contactId={id}` lists only that contact's meetings; new meeting pre-linked to the contact (`meeting?.contactId ?? contactId`) | E | ▢ (untested; `meetings-table.tsx:44-51`, `meeting-form-dialog.tsx:69`) |
| UI-18 | Create/edit meeting: datetime-local ↔ ISO UTC conversion (`toLocalInput`/`toISOString`), duration 1–1440, POST/PATCH `/crm/meetings` | E | ▢ (untested; `meeting-form-dialog.tsx:30-37,67-75`) |
| UI-19 | Complete meeting: planned row → check action → POST `/crm/meetings/{id}/complete`; list refetch + toast | E | ◐ (untested; **outcome always sent `null`** — no Outcome dialog exists, `meetings-table.tsx:89` hardcodes `outcome: null`, `hooks.ts:111-122` accepts an outcome that the UI never collects) |
| UI-20 | Cancel meeting: planned row → X action → POST `/crm/meetings/{id}/cancel`; status badge flips to `canceled` after invalidation | E | ▢ (untested; `meetings-table.tsx:93-100`) |
| UI-21 | **Standalone `/crm/meetings` page is ORPHANED**: it renders + is entitlement-gated, but no `NAV_ITEMS` entry points to it → reachable only by typing the URL | E | ◐ (`app/(tenant)/crm/meetings/page.tsx` exists; `nav.ts` lists only `/crm`, no meetings link `nav.ts:58-64`) |
| UI-22 | Entitlement-gated nav: `crm` nav item (lowercase `moduleKey:"crm"`) shown only when the tenant has the module; pages call `isModuleEnabled(ent,"crm")` → `notFound()` otherwise | E | ▢ (untested; `nav.ts:58-64`, `page.tsx:20`, casing must stay lowercase or the gate silently hides the module) |
| UI-23 | i18n: every CRM key resolves in BOTH `cs` and `en` (`crm.*`: page/table/status/meetingStatus/interactionType/filter/contacts/contactForm/interactionForm/timeline/meetings/meetingForm/toast/validation) | E | ◐ (untested by e2e, but static parity verified — identical `crm` top-level key sets in `messages/{cs,en}.json`) |
| UI-24 | Single data source + coarse invalidation: one `apiFetch` BFF client (no client-side token), one TanStack source via `crmQueries`, every mutation invalidates the whole `queryRoots.crm` root → lists/detail/timeline all refetch | E | ◐ (untested; deliberate coarse invalidation `hooks.ts:22-25` — refetches more than strictly needed, but never stale) |
| UI-25 | Delete contact: trash action → DELETE `/crm/contacts/{id}` (no confirm dialog), button disabled while pending, list refetch + toast | E | ◐ (untested; `contacts-table.tsx:83-92` — destructive action has no confirmation step) |

---

## Known issues feeding these ▢/◐

The concrete HIGH/MEDIUM defects surfaced by the line-by-line audit. Each is the root cause behind one or more
`▢`/`◐` rows above; together they are the CRM gap map.

| Sev | Issue | Where | Effect | Rows |
|---|---|---|---|---|
| **HIGH** | **PATCH contact is a full-replace, not a partial patch** — the handler writes EVERY column from the command, and the endpoint defaults omitted JSON to request defaults | `UpdateContactHandler.cs:25-33`, `UpdateContactEndpoint.cs:36`, `CreateContactEndpoint.cs:33` | A PATCH that omits `email`/`phone`/`notes`/`tags` **silently wipes them**; an omitted/blank `status` **silently downgrades** a `customer` back to `lead` | CT-10, CT-11, UI-8, DM-2 |
| **HIGH** | **CompleteMeeting is non-idempotent** — `db.ContactInteractions.Add(...)` runs on every invocation with no UNIQUE/dedup key; a second complete OR a `ConcurrencyRetryBehavior` re-run (tracker cleared) re-inserts | `CompleteMeetingHandler.cs:27` | **Duplicate `meeting` interactions** on the contact timeline from a double-submit or a transient retry | MT-20, MT-21, DM-4 |
| **HIGH** | **Contact is its own `IDataSubject` (`SubjectId => Id`)** — user-erasure shreds the USER's DEK, never each contact-row DEK, so contact PII ciphertext in `crm_audit_entries` is never crypto-shredded | `Contact.cs:43`, `CrmPersonalDataEraser.cs:19-53` | After GDPR erasure the audited contact name/email/phone stay **permanently decryptable** — violates the platform audit-PII crypto-shred guarantee | GD-14, CT-30, DM-8 |
| **HIGH** | **Free-text PII stored as plaintext in audit** — `Contact.Notes`, `ContactInteraction.Body`, `Meeting.Notes`/`Outcome` are not `[PersonalData]`, so `AuditInterceptor` writes them plaintext; the eraser's `ExecuteUpdate` bypasses the interceptor and never touches audit rows | `ContactInteraction.cs:22`, `Meeting.cs:22-25`, `CrmPersonalDataEraser.cs:24` | Original free-text PII (notes, call bodies, meeting outcomes) **survives erasure** in the audit trail | GD-15, GD-17, MT-29 |
| **MEDIUM** | **No `(UserId, CreatedAt)` index** behind the default contact sort — the list hardcodes `ORDER BY CreatedAt DESC` but the only indexes are `(UserId,Status)` and `(EmailHash)` | `ListContactsHandler.cs:46`, `Contact.cs:77-78` | The default unfiltered list does a filter-then-sort (in-memory sort node) that **degrades as a user's contacts grow** | CT-26, DM-14, DM-22 |
| **MEDIUM** | **Tags are write-only / unqueryable** — `Tags text[]` round-trips but has no GIN index and no `?tag=` filter slice; the zod client cap (512 chars) does not mirror backend per-tag rules | `Contact.cs:74`, `schema.ts:25`, `CreateContactValidator.cs:27-28` | Tags cannot drive list filtering; over-long/too-many tags pass the client and fail the server with an **invisible** error (per UI-10) | CT-28, DM-17, CT-20, CT-21, UI-11 |
| **MEDIUM** | **`no_show` meeting status is dead** — a valid `MeetingStatuses` value + an FE filter/badge, but no slice ever assigns it; relatedly there is **no state machine** so terminal transitions silently succeed | `Meeting.cs:36`, `meetings-table.tsx:23`, `Cancel/Complete/UpdateMeetingHandler` | A documented status is **unreachable**; completing a canceled meeting / cancelling a done one / rescheduling a terminal meeting all return success with no guard | MT-25, MT-22 |
| **MEDIUM** | **Orphan-on-delete** — there are NO foreign keys from `crm_meetings`/`crm_contact_interactions` to `crm_contacts`, and contact delete is soft-only with no cascade; ListInteractions never checks the parent's `DeletedAt` | (no FK in `CrmContacts.cs`/`CrmMeetings.cs` migrations), `ListInteractionsHandler.cs:9,20` | Soft-deleting a contact leaves its meetings + interactions **dangling and still listable**; a future hard-delete would orphan them outright | CT-15, IN-19, DM-21 |
| **MEDIUM** | **`Outcome` is half-wired** — the backend stores a meeting outcome but the FE never collects one: the complete button hardcodes `outcome: null` and there is no outcome dialog/field | `meetings-table.tsx:89`, `hooks.ts:111-122` | Outcomes are only reachable via a raw API caller; the product UI can never persist a meeting outcome | MT-26, UI-19 |
| **MEDIUM** | **Backend field-validation errors do not bind to forms** — the client maps `fieldErrors = problem.errors` (an **array**) and reads PascalCase `field` keys against camelCase RHF fields | `contact-form-dialog.tsx:96-101`, `meeting-form-dialog.tsx:85-90`, `client.ts:95` | Server 422 field errors **never surface per-field** in the UI; combined with UI-11 the user sees no actionable feedback | UI-10, UI-11 |
| **MEDIUM** | **Paging shape divergence** — CRM list responses are `{items,total,limit,offset}` (`total`, offset/limit, cap 200), not the platform-canonical `PagedResponse{Items,Page,PageSize,TotalCount}` | `ContactDtos.cs:25-29`, `MeetingDtos.cs:17` vs `Cqrs/Paging.cs:7` | A generic client expecting `totalCount`/1-based `page` breaks; FE only works because it reads `data.total` directly | CT-24, MT-13, UI-4 |
| **MEDIUM** | **No idempotency keys on create paths** — `AddInteraction` and `CreateContact` have no per-request idempotency key (email blind index is intentionally non-unique) | `AddInteractionHandler.cs:25-35`, `Contact.cs:28`, `CreateContactHandler.cs` | A client retry or double-submit **silently creates duplicate** interactions / contacts | DM-5, DM-6, IN-22, GD-3 |
| **LOW/MEDIUM** | **status/type are free `varchar(32)` with no DB CHECK** — membership enforced only at the validation edge | (no CHECK constraint in any CRM migration), `ContactStatuses.IsValid`/`MeetingStatuses.IsValid`/`InteractionTypes.IsValid` | Any write that bypasses the validator (a future bulk path) could persist an out-of-set value the DB would accept | DM-18, CT-19 |
| **LOW** | **Interactions are append-only with no correction path & not pageable** — only `AddInteraction`+`ListInteractions` ship; list is a flat array capped at 500 with no offset/total | `AddInteractionHandler`, `ListInteractionsHandler.cs:19-24` | A mistyped interaction is permanent; a timeline beyond 500 entries is **unreachable** (oldest silently truncated) | IN-16, IN-18 |
| **LOW** | **No `occurredAt` / `scheduledAt` date-sanity guard** — validators check only type/body and title/duration; far-past/future timestamps are accepted verbatim | `AddInteractionValidator.cs`, `CreateMeetingValidator.cs:13` | A bad `occurredAt` (year 3000) corrupts newest-first timeline ordering; past/implausible meetings are accepted | IN-13, MT-6 |
| **LOW** | **No `DeleteMeeting` / contact restore slice; no partial `DeletedAt IS NULL` index** | (no slice), plain indexes only | Meetings can only be erased via GDPR; no admin un-delete; tombstoned rows are still scanned by the soft-delete filter | MT-27, CT-16, DM-22 |
| **LOW** | **No CRM e2e coverage at all** — `frontend/e2e/` has no `crm.spec.ts`; every UI-row is `▢`/`◐` | `frontend/e2e/` | The entire FE slice (forms, paging, complete/cancel, delete-without-confirm, i18n, orphaned `/crm/meetings` page) is **unproven end-to-end** | UI-1…UI-25 |